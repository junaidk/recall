.git
.gitignore
.claude
.idea
.vscode

# Local data & build output — never bake into the image.
data
extra
target
build-conjugations
seed-export
*.db
*.db-journal
*.db-wal
*.db-shm

# Host config (mounted at runtime, never built in).
config.yaml

# Docs & meta — irrelevant to the binary.
docs
README.md
deploy/README.md
